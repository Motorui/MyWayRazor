@page
@model MyWayRazor.Pages.Tabelas.Contratos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Contratos</h2>

<p>
    <a asp-page="Create">Criar novo registo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Contrato[0].ContratoTipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contrato[0].CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contrato[0].CreatedBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contrato[0].LastUpdatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contrato[0].LastUpdatedBy)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Contrato) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContratoTipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdatedBy)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ContratoId">Editar</a> |
                <a asp-page="./Details" asp-route-id="@item.ContratoId">Detalhes</a> |
                <a asp-page="./Delete" asp-route-id="@item.ContratoId">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
