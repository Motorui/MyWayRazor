@page
@model MyWayRazor.Pages.Tabelas.Statuses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Status[0].Statuses)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status[0].CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status[0].CreatedBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status[0].LastUpdatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status[0].LastUpdatedBy)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Status) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Statuses)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdatedBy)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.StatusID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.StatusID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.StatusID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
