@page
@model MyWayRazor.Areas.Identity.Pages.Admin.DetailsModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<div>
    <h4>User</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.UserData.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.UserData.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.UserData.Email)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.UserData.Email)
        </dd>
        @foreach (var item in Model.UserData.UserRoles)
        {
            <dt>
                @Html.DisplayNameFor(modelItem => item.Role.Name)
            </dt>
            <dd>
                @Html.DisplayFor(modelItem => item.Role.Name)
            </dd>
        }
        </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.UserData.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
